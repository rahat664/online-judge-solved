#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while(t--)
    {
        int num, ans;
        cin >> num;
        ans = sqrt(num);
        cout << ans << endl;
    }
}
